import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        padding: 20, 
        alignItems: 'center'
    },

    user:{
        marginTop: 10, 
        fontSize: 18, 
        fontWeight: 'bold'
    },

    logOut:{
        padding: 15, 
        marginTop: 20, 
        backgroundColor: '#E85D04', 
        alignItems: 'center',
        borderRadius: 12
    },
    
    textLogOut:{
        color: '#fff', 
        fontWeight: 'bold'
    }
})