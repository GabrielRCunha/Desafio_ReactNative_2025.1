import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        width: 300,
        marginBottom: 500
    },

    descriptionTitle:{
        color: "white",
        fontSize: 15,
        fontFamily: "RockSalt"
    },

    descriptionText:{
        fontSize: 15,
        color: "white"
    }
})